<UserControl x:Class="SchoolERP.Views.ReceiptsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
        <Style x:Key="IconButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="34"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style x:Key="ReceiptGridStyle" TargetType="DataGrid">
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#E2E8F0"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#F8FAFC"/>
            <Setter Property="RowHeight" Value="44"/>
            <Setter Property="ColumnHeaderHeight" Value="40"/>
            <Setter Property="HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,18">
            <TextBlock Text="Receipts" FontSize="24" FontWeight="Bold" Foreground="#0F172A"/>
            <Button Content="Refresh"
                    Command="{Binding LoadCommand}"
                    DockPanel.Dock="Right"
                    Width="110" Height="38"
                    Background="#001A3F" Foreground="White"
                    BorderThickness="0" Cursor="Hand"/>
        </DockPanel>

        <DockPanel Grid.Row="1" Margin="0,0,0,12" LastChildFill="False">
            <TextBlock Text="Search" Foreground="#64748B" FontWeight="SemiBold"
                       VerticalAlignment="Center" Margin="0,0,8,0"/>
            <Grid Width="420">
                <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Height="38" Padding="34,0,10,0" VerticalContentAlignment="Center"
                         ToolTip="Search receipt number, student, registration number, class, date or fee details"/>
                <TextBlock Text="&#xE721;" FontFamily="Segoe MDL2 Assets" FontSize="15"
                           Foreground="#64748B" Margin="10,0,0,0" VerticalAlignment="Center"
                           IsHitTestVisible="False"/>
            </Grid>
        </DockPanel>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Receipts}" MinHeight="300"
                  Style="{StaticResource ReceiptGridStyle}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Receipt No" Binding="{Binding ReceiptNumber}" Width="170"/>
                <DataGridTextColumn Header="Student" Binding="{Binding StudentName}" Width="*" MinWidth="140"/>
                <DataGridTextColumn Header="Reg No" Binding="{Binding RegistrationNo}" Width="130"/>
                <DataGridTextColumn Header="Class" Binding="{Binding ClassName}" Width="100"/>
                <DataGridTextColumn Header="Section" Binding="{Binding Section}" Width="75"/>
                <DataGridTextColumn Header="Payment Date" Binding="{Binding PaymentDateDisplay}" Width="120"/>
                <DataGridTextColumn Header="Amount" Binding="{Binding AmountPaid, StringFormat='Rs {0:N0}'}" Width="110"/>
                <DataGridTextColumn Header="Details" Binding="{Binding Details}" Width="2*" MinWidth="180"/>
                <DataGridTemplateColumn Header="Actions" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Style="{StaticResource IconButtonStyle}" Click="ViewReceipt_Click" ToolTip="View receipt">
                                <TextBlock Text="&#xE890;" FontFamily="Segoe MDL2 Assets" FontSize="16" Foreground="#001A3F"/>
                            </Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <DockPanel Grid.Row="3" Margin="0,10,0,0">
            <TextBlock Text="{Binding StatusMessage}" Foreground="#64748B"/>
            <TextBlock Text="Loading..." DockPanel.Dock="Right" Foreground="#64748B"
                       Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}"/>
        </DockPanel>
    </Grid>
</UserControl>
