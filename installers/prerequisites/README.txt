Place these official offline installers in this folder before building:

1. ndp48-x86-x64-allos-enu.exe
   Microsoft .NET Framework 4.8 offline installer.

2. SQLEXPR_x64_ENU.exe
   Microsoft SQL Server Express x64 installer.

They are embedded into the final SchoolERP setup executable. Do not rename them.
